import { Component } from "react";
import PropTypes from "prop-types";

export default class VciDetect extends Component {
   static propTypes = {
      prop: PropTypes,
   };

   render() {
      return <div></div>;
   }
}
